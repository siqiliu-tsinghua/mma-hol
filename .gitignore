*.mx
*.mx/
.WolframEngine/
.wolframscript/
*.nb~
*.nb.bak
Untitled-*.nb
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*~

# external Lean reference project (the unpublished "tautology" Dedekind ℝ blueprint).
# Kept ignored as a safety guard: if the private blueprint is ever re-extracted here for
# reference, this prevents it from being committed to the public repo.
tautology-ref/
