# mma-hol — 按阶段的详细证明进度（proof-history archive）

本文件保存逐阶段的详细证明叙述，于 2026-06-02 从 `CLAUDE.md` 的 "Current state" 段移出，以保持后者精简。
**权威细节仍以 `git log` 的 commit message + 代码注释为准**；本文件是便于人类检索的汇总。`CLAUDE.md` 只保留高层状态 + 当前续作锚点（NEXT TASK）。

---

## M1–M6（kernel / derived rules / tactics / parser+printer）

M1–M6 done (kernel, derived rules, tactics, parser+printer; **M6b notebook front-end CUT 2026-05** — MakeBoxes thm-typeset / term→Box 2D / goal-panel all dropped; demo `.nb` retained as `Needs` toplevel + prove-a-signature-theorem with text output).

---

## M7 automation（MESON / SIMP / SET / ARITH / REAL_ARITH）

**M7 automation** — `MESON` (auto/Meson.wl) and `SIMP`/`asmSimp` (auto/Simp.wl) substantially complete; `SET` (auto/Set.wl) done. `ARITH` (auto/Arith.wl) **linear ℕ working (∀/∃/=, atoms)** — ∃-SAT prover (S9-11); ∀/open goals close via a Fourier–Motzkin oracle + kernel verifier with non-unit integer multipliers, implicit ℕ-nonnegativity, atom abstraction of non-arithmetic subterms (nonlinear `m·n`, `LENGTH l`), and `=`-atoms (conclusions via antisymmetry, =-hyps split). Remaining ARITH work is narrow (see below). Pending: MESON α-5-c Brand modulation + NOT_FORALL/NOT_EXISTS NNF schemas (deferred till a goal forces them); `REAL_ARITH` (M7-ε) not started.

**M7-δ sessions 1–10 done (auto/Arith.wl)** — Cooper QE pipeline as untrusted algorithm + kernel-checked ∃-SAT prover. linTerm AST + HOL↔linTerm (`parseLin`/`buildLin`); Atom/Formula AST + algorithmic `nnfForm`; HOL NNF certificate `nnfConv` (9 schemata via `propTaut`, fixpoint `DEPTHCONV`); quantifier deMorgan `notExistsNumThm`/`notForallNumThm` at numTy; Cooper scaffolding `normalizeAtom`/`linCoefOf`/`deltaOnX`/`phiMinusInfOnX`/`bSetOnX`/`cooperExistsStep`; AST simplifier `simpForm` (ground-atom evaluation + T/F folding); ground provers `proveGroundAddEq`/`MultEq`/`Leq`/`Lt`/`Divides` + `proveGroundReduceTm` for compound atoms; `arithProveExists` orchestrates witness search → BETACONV-substitute → ground proof → EXISTS lift. **Stage detail: see `git log` from `d599429`..`c216119`.**

**M7-δ session 11 done** — public entry points `arithProve[goalTm]` + `ARITH[][goal]` tactic. `arithProve` dispatches ∃x:num.body → `arithProveExists` (others → `arith-not-supported`); `arithProveExists` recurses via `arithProveBody` so nested ∃ closes automatically. ARITH usable via `prove[goal, ARITH[]]`. End-to-end on `∃x. x=5`, `∃x.∃y. x≤y`, `∃x.∃y. x=3 ∧ y=7`. ∀-goals still queued.

**M7-δ ARITH = oracle+verifier (Farkas), NOT internal Cooper QE.** Cooper line **cut 2026-05-28** — the S5-6 -∞ scaffolding (`phiMinusInfOnX`/`bSetOnX`/`cooperExistsStep`) assumes ℤ; over ℕ the floor at 0 makes the main-theorem equivalence false (`∃x. x+5≤3` is F but cooperExistsStep gives T). Design rationale + counterexample in memory `project_arith_oracle_verifier`. PLAN.md line 464 always specified oracle (WL `LinearProgramming` → Farkas certificate) + verifier (kernel-assembles `+`/`≤` monotonicity lemmas); **ℕ-native, no ℤ needed**. ∃-goals: S9-11 witness-search + ground-verify (kept). ∀/UNSAT: Farkas infeasibility certificate + verifier (new). Scope: **linear only** (no nonlinear `m*n`, no divisibility-QE). Capstone: `∀m n p. m ≤ n ⇒ m+p ≤ n+p`.

**Still live in the kept ∃-SAT path (S9-11):** the ∃-SAT prover (`arithProve`/`arithProveExists`/`findSatWitness`/`candidateIntegers`) reuses most of the Cooper AST scaffolding for *candidate generation + witness checking* — `linTerm`/`parseLin`/`parseAtom`, `normalizeAtomsForm`, `deltaOnX`, `bSetOnX`, `substVarInForm`, `simpForm` — plus `nnfConv` and the ground provers (S8). These are NOT dropped. Num-side building blocks: `existsBoundedThm` (S12), divides-periodicity `dividesAddEqThm`/`dividesShiftThm` (S13-14), the leq verifier lemmas (batches 1-3). **Orphaned (still defined + tested in arith_tests, but no live caller after the realign — remove when convenient):** `cooperExistsStep` + `phiMinusInfOnX` (the would-be main-theorem AST). The old S15-S18 Cooper roadmap is abandoned.

**M7-δ verifier lemma set DONE (stdlib/Num.wl).** Batch 1 additive monotonicity: `leqAddRightMonoThm` (= capstone `∀m n p. m≤n ⇒ m+p≤n+p`), `leqAddLeftMonoThm`, `leqAddMonoThm`. Batch 2: scaling `leqMultLeftThm` (`a≤b ⇒ k*a≤k*b`), left-cancel `leqAddLeftCancelThm` (`v+a≤v+b ⇒ a≤b`). Batch 3 order-negation: `notLeqEqLtThm` (`¬(m≤n)=(n<m)`), `notLtEqLeqThm` (`¬(m<n)=(n≤m)`). Strict-monotonicity lemmas deferred — engine converts `<` to `SUC·≤` and combines in ≤-land.

**M7-δ Farkas ∀-engine DONE (auto/Arith.wl).** `linNormConv[t]` → `⊢ t = buildLin[parseLin[t]]` (proof-producing linear normalizer: caAdd flattens via addAssoc + folds monomials via caInsert with coefficient-merge `k·x+cx·x=(k+cx)·x`; sucEqAddOne reduces SUC-of-sum; recognizes `klit·x` monomials; covers sort + const-fold + SUC + merge). `arithProveForall`: peel ∀ (origin names reused → GEN reconstructs goal exactly) + ⇒, ASSUME hyps, negate concl (notLeqEqLt/notLtEqLeq), `refuteWithNonneg` → F, CCONTR/DISCH/GEN. Oracle = **Fourier–Motzkin** (`farkasFM`, untrusted, exact rationals — fmConstr tracks the prov combination through variable elimination, no external LP, mirrors HOL Light); returns integer multipliers. `farkasRefute` applies λᵢ by **repeated leqAddMono** (`repeatLeqFact`, not leqMultLeft — keeps terms in the `+` world so only merge is needed), normalizes (linNormConv), commutes const right, cancels shared var (leqAddLeftCancel), ground-contradicts (notLeqEqLt + proveGroundLt → NOTELIM). `refuteWithNonneg` lazily retries with implicit `⊢ 0≤xᵢ` (leqZeroThm) on a no-cert miss → ℕ truths like `∀x. x≤x+x` close. `arithProve` dispatcher routes ∃x:num to arithProveExists and ∀x:num / implication / bare ≤/</num-= atoms (open subgoals) to arithProveForall. **Atom abstraction:** a dynamically-scoped `$atomEnv` (Block per arithProveForall call) lets parseLin abstract a maximal opaque num subterm (nonlinear `m·n`, `LENGTH l`) to a fresh key; buildLin + normalizer materialize key→term via `atomTermFor`; normLinAux REFLs a term that is its own canonical form (`buildLin∘parseLin === t`). The verifier lemmas are ∀-polymorphic over num, so atoms flow through unchanged and GEN binds inner vars inside an atom. **`=`-atoms:** `proveConclUnderHyps` proves an `=` conclusion by antisymmetry from the two ≤ directions (`leqAntisymThm`); `hypToLeqFacts` splits an `=` hypothesis into both ≤ via `eqToLeqFwd`. Proven: capstone, non-unit `∀x. 1≤x ⇒ 2≤x+x` (λ=2), `∀m n. m≤m+n`, `∀m n. m·n ≤ m·n+1` (atom), `∀m n. m+n = n+m` (commutativity); rejects false `∀x. x≤0`.

`auto/Arith.wl` now loads **right after `stdlib/Num.wl`** (before List/Finite/FTA) so stdlib can call it; first in-tree consumer is Finite.wl's `nsumStepCommBuilder` (NSUM commutativity via `arithProve`). ARITH's main consumers are still M8 (analysis index/interval arithmetic) — the rest of stdlib is structural equalities with no ARITH targets.

---

## M7 stdlib through M7-5（Pair … FTA）

**M7 stdlib done through M7-5 (`stdlib/Int.wl` COMPLETE)** — Pair, Sum, Option, Set, Num (Peano/`+`/`×`/`^`/`≤`/`<`/strong induction/well-ordering/`DIV`/`MOD`/`divides`/`gcd`/`prime`/Euclid; no Bezout), List (`α list` as `num→α option` finite-support subtype; `LENGTH`/`HD`/`TL`/`FOLDR`/`FOLDL`/`APPEND`/`MAP`/`FILTER`/`ALL`/`MEM`), Finite (`INSERT`/`SING`/`DELETE` + inductive `FINITE` + 4 closure lemmas + FINREC/ITSET/FINITE_RECURSION + `CARD`/`NSUM`), FTA (`primeFactorsExistsThm` + `primeFactorsUniqueThm` modulo `PERM`). `COND` + COND_CLAUSES in Bool.wl. **Stage detail: see `git log` from commit `b5a4c40`..`c216119`.**

---

## M7-5 `stdlib/Int.wl` — ordered integral domain（stages a–h + cancellation）

**M7-5 `stdlib/Int.wl` COMPLETE (ordered integral domain, stages a–h + cancellation).** Per PLAN.md line 465: int via canonical representatives over `num × num` (NOT a K_0 quotient, NOT axiom-Peano-ℤ). Prereq `monus ∸` + `PRE` on num (in Num.wl; `monusZeroThm`/`monusSucThm`/`monusSucSucThm`/`addMonusCancelThm`/`monusSelfThm`/`leqAddMonusThm`). Type carved by `INT_REP = λp. FST p = 0 ∨ SND p = 0` via `newBasicTypeDefinition` → `ABS_int`/`REP_int` + `absRepIntThm`/`repAbsIntThm`. **Embedding** `&ℤ : num → int = λn. ABS_int (n,0)` with `intRepNatPairThm`/`repIntOfNumThm`/`intOfNumInjThm`. **Negation** `intNeg z = ABS_int (SND(REP z), FST(REP z))` (swap stays canonical) with `intRepRepThm`/`intRepSwappedThm`/`repIntNegThm`/`intNegNegThm`; this needed **`pairSurjThm: ∀p. (FST p, SND p) = p`** added to Pair.wl (reusable). **Canonicalizer** `intCanon = λp. COND (SND p ≤ FST p) (FST p ∸ SND p, 0) (0, SND p ∸ FST p)` (via `EXCLUDEDMIDDLE` on guard + `eqFIntro` helper for the F branch) with `intRepCanonThm: INT_REP(intCanon p)` and `intCanonIdThm: ∀p. INT_REP p ⇒ intCanon p = p` (canon idempotent on canonical, by case-split on the carve disjunction). **Addition** `intAdd z w = ABS_int (intCanon (FST(REP z)+FST(REP w), SND(REP z)+SND(REP w)))` with `intAddCommThm` (falls out of num `addCommThm`), `intAddZeroThm: ∀z. intAdd z (&ℤ 0) = z` (right identity, via canon-idempotent on `REP z`), and `intAddNegThm: ∀z. intAdd z (intNeg z) = &ℤ 0` (right inverse — REP(intNeg z) swap makes the summed pair (c,c) with c = FST R + SND R via `addCommThm`; intCanon (c,c) → (c∸c,0) → (0,0) via `leqReflThm`+`monusSelfThm`; ABS_int(0,0) = &ℤ 0). **`intAddAssocThm: ∀z w v. intAdd (intAdd z w) v = intAdd z (intAdd w v)`** (associativity) closes the **additive abelian group**. Built the Grothendieck-equivalence layer over `num × num` ((a,b) ≈ (c,d) :⟺ a+d=c+b): `canonEquivThm` (intCanon p ≈ p), `canonInjThm` (canonical reps unique — 4-way carve split, each leaf's num equalities via ARITH on FST/SND atoms + pairSurj assembly), `canonRespectsThm` (a+d=c+b ⇒ intCanon(a,b)=intCanon(c,d), = canonEquiv+canonInj+ARITH), `repIntAddThm` (REP(intAdd z w) = intCanon(z1+w1,z2+w2), reusable for mult). assoc itself: both sides → ABS_int(intCanon(SUM)) via repIntAddThm; inner canon-of-canon collapses through canonRespects (ARITH-derived equivalence); SUM pairs align by addAssoc. **This is the first in-tree exploitation of ARITH from stdlib/Int** — FST/SND atom abstraction lets 3-deep `=`-implications over opaque atoms close automatically.

**`intSucc`/`intPred`** = `z ± 1ℤ` (`intSucc z = intAdd z (&ℤ 1)`, `intPred z = intAdd z (intNeg (&ℤ 1))`) with round-trips `intPredSuccThm`/`intSuccPredThm` (`∀z. intPred(intSucc z) = z` and converse) — mutually-inverse bijections falling out of assoc + inverse (+ comm) + identity; the working tool for bidirectional int induction.

**M7-5e multiplication DONE — ℤ is a commutative ring.** `intMul z w = ABS_int(intCanon(z1*w1+z2*w2, z1*w2+z2*w1))` (the pair product `(a−b)(c−d)=(ac+bd)−(ad+bc)`). All of: `repIntMulThm` (mirrors repIntAddThm), `intMulCommThm`, `intMulOneThm` (`intMul z (&ℤ 1) = z`), `intMulZeroThm`, `intMulDistribThm` (left distributivity), `intMulAssocThm` (associativity). **Key contrast with add: products are opaque to ARITH** (it abstracts `a*b` as an atom and doesn't know commutativity/distributivity), so the num glue is explicit `timesComm`/`timesZero`/`oneTimes`/`timesDistrib`/`timesAssoc` rewriting — ARITH only helps once products are already expanded into a linear shape. Distrib + assoc reuse the `canonRespects` bridge of intAddAssoc: each side → `ABS_int(intCanon ·)` via a scaled+distributed `canonEquivAt` fact (helpers `scaleDistribEquiv` = left-scale/`timesDistribLeft`, `scaleDistribEquivRight` = right-scale/`timesDistribRight`). For assoc the two sides' distributed pairs hold the same monomials in different association `(zi*wj)*vk` vs `zi*(wj*vk)`; they're reconciled by feeding ARITH the eight `timesAssoc` equalities as antecedents (all reassociation localized to two ARITH calls).

**ℤ is an INTEGRAL DOMAIN.** `intMulEqZeroThm: ∀z w. intMul z w = &ℤ 0 ⇒ z = &ℤ 0 ∨ w = &ℤ 0` (no zero divisors). Proof: REP(intMul z w)=(0,0) ⇒ (canonEquivAt) `z1*w1+z2*w2 = z1*w2+z2*w1`; 4-way carve case-split, each leaf reduces (vanishing products + ARITH) to one surviving product = 0, then **Num `multEqZeroThm`** (`∀m n. m*n=0 ⇒ m=0∨n=0`, a fundamental ℕ fact — moved here from an FTA stage-3.d helper; FTA's `multLeftCancelThm` now points at Num's) + helper `intEqZeroFrom` (`FST(REP x)=0 ∧ SND(REP x)=0 ⇒ x=&ℤ0`). The ℚ-facing **cancellation** form (`z≠0 ⇒ z*w=z*v ⇒ w=v`) is NOT yet done — it needs mul-neg (`intMul z (intNeg v) = intNeg(intMul z v)`, via a canon-swap lemma) + subtraction; do it just before/at ℚ.

**M7-5f order — ℤ is an ordered abelian group.** REP-based, subtraction-free: `intLe z w ⟺ z1+w2 ≤ w1+z2`, `intLt z w ⟺ z1+w2 < w1+z2`. Linear-order axioms `intLeReflThm`/`intLeAntisymThm`/`intLeTransThm`/`intLeTotalThm` + `intLtNotLeThm` (`intLt z w = ¬(intLe w z)`); antisymmetry uses `canonInjThm` (equal-sum hyp ⟹ reps coincide), the rest are ℕ order lemmas + ARITH over FST/SND atoms. Compatibility: `intLeAddMonoThm` (`intLe z w ⇒ intLe (intAdd z u) (intAdd w u)`, via canonEquivAt at the summed pairs + ARITH), `intLeNegThm` (`intLe z w ⇒ intLe (intNeg w) (intNeg z)`, via repIntNegThm swap + ARITH), and `intLeMulNonnegThm` (`intLe (&ℤ 0) u ⇒ intLe z w ⇒ intLe (intMul u z) (intMul u w)`). **ℤ is now an ordered ring.** mult-mono's multiplicative core is the new **Num `crossMultLeqThm`** (`∀a b p q. b≤a ⇒ p≤q ⇒ a*p+b*q ≤ a*q+b*p`, ARITH-free since Num precedes Arith — witness + distrib + leqMultLeft/leqAddLeftMono); the int proof reduces the goal (canonEquivAt) to that cross inequality at `(u1,u2,z1+w2,w1+z2)` with products-of-sums distributed, ARITH gluing.

**M7-5g done — `&ℤ` is a ring/order embedding + `intAbs`.** `intOfNumAddThm` (`&ℤ(m+n)=intAdd(&ℤm)(&ℤn)`), `intOfNumMulThm` (`&ℤ(m*n)=intMul…`), `intOfNumLeThm` (`intLe(&ℤm)(&ℤn) = (m≤n)`) — REP(&ℤk)=(k,0) is canonical so the op pair collapses to `(m+n,0)`/`(m*n,0)` via intCanonId; order needs no canon. `intAbs z = &ℤ(FST(REP z)+SND(REP z))` (one component is 0 ⇒ sum is |z|) with `intAbsNumThm`/`intAbsNegThm`/`intAbsNonnegThm`.

**M7-5h done — bidirectional integer induction; ℤ construction (stages a–h) COMPLETE.** `intInductionThm: ⊢ ∀P. P(&ℤ0) ∧ (∀z. P z ⇒ P(intSucc z) ∧ P(intPred z)) ⇒ ∀z. P z`. Supporting: `negUniqueThm` (inverses unique), `intNegAddThm` (`−(z+w)=−z+−w`), `intNegSuccThm` (the dihedral `−(z+1)=(−z)−1`, **the one use of RSR=S⁻¹**), `intSuccOfNumThm` (`intSucc(&ℤn)=&ℤ(SUC n)`), `intNegZeroThm`, `intCasesThm` (sign split). Proof: `numInductionThm` twice (ISPEC + CONVRULE-BETACONV) — up through `&ℤ n` via intSucc, down through `intNeg(&ℤ n)` via intPred; `intCases` dispatches an arbitrary z.

**int cancellation done.** `intMulNegThm` (`intMul z (intNeg v) = intNeg(intMul z v)`, pure group route via negUnique — no canon-swap) and `intMulCancelThm` (`¬(z=&ℤ0) ⇒ intMul z w = intMul z v ⇒ w = v`, via distrib+mul-neg+intMulEqZero+negUnique+intNeg involution). **`stdlib/Int.wl` is now feature-complete for ℚ.**

---

## M7-6 `stdlib/Rat.wl` — ℚ field（stages a–e DONE）

**M7-6 `stdlib/Rat.wl` IN PROGRESS — stages a+b+c done (a+b `da640f6`; c committed through `2712b1a`, cold-verified 1861/0).** ℚ = **canonical reduced fractions** carved from `int × num` (numerator int, denominator positive num) by `RAT_REP = λp. ¬(SND p = 0) ∧ gcd (intNatAbs (FST p)) (SND p) = SUC 0` — kernel `=` IS rational equality (no setoid; user chose this over a `ratEq` setoid, 2026-05-31). Stage a: `intNatAbs : int→num`, `RAT_REP` carve + `ABS_rat`/`REP_rat` + witness `RAT_REP(&ℤ0, SUC 0)`; ℕ helpers `dividesZeroImpZeroThm`/`dividesOneThm`/`gcdOneRightThm` (proper home Num.wl, kept in Rat pending migration). Stage b: `&ℚ : int→rat` (q↦q/1) + `ratRepOneDenomThm`/`repRatOfIntThm`/`ratOfIntInjThm` (mirrors Int `&ℤ`). **Stage c** (`ratCanon` gcd-reduction): `ratCanon p = (intDivNat (FST p) g, exDiv (SND p) g)`, `g = gcd(intNatAbs(FST p))(SND p)`, with capstones `ratCanonLandsThm` (`¬(SND p=0) ⇒ RAT_REP(ratCanon p)`) + `ratCanonIdThm` (`RAT_REP p ⇒ ratCanon p = p`). **Design deviation from the planned `DIV` route:** exact quotient `exDiv n g = ε c. n=g·c` (Hilbert-ε on the divides witness) replaces `DIV`, so `exDivThm` (`divides g n ⇒ n=g·exDiv n g`) falls straight out of selectAx, skipping all MOD-zero reasoning. (The DIV route is *not* actually heavy — exact division needs only `n MOD g = 0`, which reduces to `divides g (g·q+r) ⇒ divides g r` (`dividesAddRightThm`) + `r<g ∧ divides g r ⇒ r=0` (`dividesLeqThm`), ~12 lines, NOT a general division-uniqueness lemma. exDiv's real win is smaller: no MOD reasoning at any use site, and idempotency `exDiv n 1 = n` is immediate rather than via `n MOD 1 = 0`.) `exDivOneThm`/`exDivZeroThm` likewise trivial. Cancellation throughout is ℕ-side (`multLeftCancelThm`), not ℤ integral-domain. Supporting ℕ number theory (proper home Num.wl, kept in Rat pending migration): `coprimeReducedThm` (`¬(gcd a b=0) ⇒ gcd(exDiv a (gcd a b))(exDiv b (gcd a b))=SUC 0` — the crux, via `g·h | g ⟹ h·k=SUC 0 ⟹ h=SUC 0`), `dividesMultBothLeftThm`, `gcdNonzeroFromRightThm`. Int÷nat layer `intDivNat z g = ABS_int(exDiv(FST(REP z))g, exDiv(SND(REP z))g)` with `repIntDivNatThm`/`intDivNatOneThm`/`intNatAbsIntDivNatThm` (carve case-split keeps the 0-component 0 so the divided pair stays canonical). 44 dev assertions green via `tests/dev_rat.wls` (TEMP harness, untracked: restore Int snapshot + `Get` Rat on top; `bootstrap.mx` deliberately kept WITHOUT Rat so dev_rat keeps working — Rat IS registered in run_all/run_all_stable/build_snapshot). **ARITH gap found:** `arithProve` rejects `¬(=)` hyps (`toLeqFact` throw) — convert via `ltZeroNotZeroThm` to `0<·` first. See memory `project_rat_construction`. **Stage d (ratAdd) — additive abelian group; structural laws DONE (60 dev assertions).** `ratAdd q r = ABS_rat(ratCanon(intAdd (intMul a (&ℤ d)) (intMul c (&ℤ b)), b·d))` over reps (a,b)=REP q, (c,d)=REP r; foundations `ratRepRepThm` (RAT_REP(REP q), mirror intRepRep), `multNonzeroThm`, `repRatAddThm` (REP of sum = reduced sum-pair, lands via ratCanonLands), `ratEqFromRepEq` (REP injective). Done: `ratAddCommThm` (sum-pairs literally equal — intAddComm+timesComm — so ratCanon of equal args), `ratAddZeroThm` (structural: ratCanonId collapses the sum back to REP q), `ratNeg`+`repRatNegThm` (numerator sign-flip, lands with no reduction; helper `intNatAbsNegThm`).

**M7-6 BEZOUT NUMBER-THEORY CORE DONE + COLD-VERIFIED (2026-06-01, commits `2057114`→`9e583c5`→`38c3a48`/`8e199a4`→`61604c4`→`1bbc8f3`; cold Strict run_all 1887/0, dev_rat 70/0).** Five ℕ lemmas (proper home Num.wl, kept in Rat pending migration) gating ℚ lowest-terms uniqueness: `dividesAntisymThm` (`divides a b ⇒ divides b a ⇒ a=b`, EXCLUDEDMIDDLE on b=0), `gcdZeroRightThm` (`gcd a 0 = a`), `gcdRecThm` (`¬(b=0) ⇒ gcd a b = gcd b (a MOD b)`, mutual-divides via divisionPair kept ADDITIVE + dividesAddRight/dividesAdd/dividesMultRight + gcdUniversal + dividesAntisym; a→(b·q+r) rewrites via APTERM/EQMP not SUBS since `a` recurs in `gcd a b`), `bezoutNatThm` (`∃x y. a·x = b·y + gcd a b ∨ b·y = a·x + gcd a b`, strong induction on modulus b via gcdRec — **monus NOT needed**, the planned monus route was overcautious: keep `a=b·q+r` additive, ·y0, both disjuncts share witnesses X=y0,Y=q·y0+x0), `coprimeDividesProductThm` (ℕ Gauss: `gcd a b=SUC 0 ⇒ a|(b·c) ⇒ a|c`, Bezout ·c + dividesAddRight). **Pitfall hit+fixed (commit `1bbc8f3`):** `plusC[]` bare helper was defined only in the later intNatAbs section, so bezout's load-time use left it unevaluated → destFunType; hoisted into the early Bezout-helper block (lines ~133-141, alongside `ltTmR`/`divTmR`/`modTmR`/`orCR`/`orTmR`/`existsCR`/`existsTmR`/`dividesHead`).

**M7-6 ℚ LOWEST-TERMS UNIQUENESS + ADDITIVE INVERSE DONE + COLD-VERIFIED (2026-06-01; cold Strict run_all 1903/0, dev_rat 86/0).** `ratEqCrossThm` (`⊢ ∀q r. (q = r) = (intMul (FST(REP q)) (&ℤ(SND(REP r))) = intMul (FST(REP r)) (&ℤ(SND(REP q))))` — cross-mult; backward mirrors Int `canonInjThm`: magnitudes via `intNatAbsMulOfNumThm` give `|a|·d = |c|·b`, then `b∣|a|·d` with `gcd(|a|,b)=1` ⟹ `b∣d` (ℕ Gauss `coprimeDividesProductThm`), symmetric ⟹ `b=d` (`dividesAntisymThm`), then `intMulCancelThm` ⟹ `a=c`; iff assembled by `DEDUCTANTISYM`). `ratAddNegThm` (`⊢ ∀q. ratAdd q (ratNeg q) = &ℚ(&ℤ0)` — sum-pair numerator `a·&ℤb + (−a)·&ℤb → &ℤ0` via `intMulNegThm`+`intAddNegThm`, so `REP = ratCanon(&ℤ0, b·b) = (&ℤ0, SUC0) = REP(&ℚ(&ℤ0))`). Supporting ℕ/int lemmas (proper home Num/Int.wl, kept in Rat pending migration): `gcdCommThm`, `intNatAbsMulOfNumThm` (`|z·&ℤn| = |z|·n` — multiplying a canonical rep by a nonneg int keeps it canonical, no intCanon monus), `gcdZeroLeftThm`, `exDivSelfThm` (`¬(m=0)⇒exDiv m m=SUC 0`), `intDivNatZeroThm`, `ratCanonZeroNumThm` (`¬(m=0)⇒ratCanon(&ℤ0,m)=(&ℤ0,SUC0)`). **Closes additive-group comm + identity + inverse.**

**M7-6 ℚ ADDITIVE ABELIAN GROUP CLOSED — `ratAddAssocThm` DONE + COLD-VERIFIED (2026-06-01; cold Strict run_all 1919/0, dev_rat 102/0).** Built the **ratCanon-respects tower** (the cross-multiplication analog of Int's Grothendieck `canonRespects`, one level up — `(a,b) ≈ (c,d) :⟺ a·&ℤd = c·&ℤb`), 8 lemmas: (1) `intMulDivNatCancelThm` (Int-level, "proper home Int.wl": `∀z g. ¬(g=0) ⇒ divides g (intNatAbs z) ⇒ intMul (intDivNat z g) (&ℤ g) = z` — `repIntMul` expansion + INT_REP carve split so each surviving `(exDiv zi g)·g = zi`, then intCanonId); (2) `ratCanonEquivThm` (`ratCanon p ≈ p`; short given (1) via `intOfNumMul`+reassoc+cancel); (3) `ratCanonInjThm` (cross-equal canonical pairs are equal — `ratEqCross`'s backward direction lifted to arbitrary pairs); (4) `ratCrossTransThm` (`≈` transitive, cancels the shared middle `&ℤd` via `intMulCancelThm`); (5) `ratCanonRespectsThm` (= equiv∘inj∘trans); (6/7) `ratAddCongLeftThm`/`ratAddCongRightThm` (`ratCanon` of the sum-pair is invariant under swapping one operand for a cross-equivalent one — polynomial core reduces by factoring the shared `&ℤm2`, then `intRDistAt`/`crossSwapAt` + one hyp; right = left between flipped pairs). **`ratAddAssocThm`** (`∀q r v. ratAdd (ratAdd q r) v = ratAdd q (ratAdd r v)`): both sides → `ratCanon` of a sum-pair on a *reduced* inner sum (`repRatAdd`); cong-left/right swap that for the *unreduced* cross-equivalent sum (`repAddEquivAt`: REP(ratAdd q r) ≈ its unreduced pair); the two fully-unreduced pairs are **literally equal** after `intAddAssocThm` (numerator MID-form `aT+(cT+eT)`) + int-ring (`intOfNumMul`/comm) + denom `timesAssocThm` — no further cancellation. Pitfall: multi-eq `SUBS` to build the unreduced cross-eq corrupts the `m1·m2` *inside* a freshly-introduced `FST suml` — build it by explicit `TRANS` instead. Reusable engine `crossSwapAt`/`intRDistAt`/`sumPairTm`/`sumFlipAt`/`repAddEquivAt`/`amA` now in Rat.wl for the upcoming ratMul well-definedness/distrib/assoc.

**M7-6 ℚ IS A FIELD — stage e (`ratMul` + `ratInv`) DONE + COLD-VERIFIED (2026-06-02; cold Strict run_all 1953/0, dev_rat 136/0; commits after `845fd2f`).** `ratMul q r = ABS_rat(ratCanon(intMul a c, b·d))` over the int×num reps. `repRatMulThm` (mirror repRatAdd), `ratMulCommThm` (product-pairs literally equal — intMulComm+timesComm), `ratMulOneThm` (right identity `&ℚ(&ℤ(SUC 0))`: pair → REP q via intMulOne + `b·SUC0=b`, ratCanonId), `ratMulZeroThm` (absorb `&ℚ(&ℤ0)` via intMulZero + ratCanonZeroNum). **Product-pair cong tower** (analog of the add tower): `mulPairTm`/`mul4SwapAt` (`(w·x)·(y·z)=(w·y)·(x·z)`)/`mulFlipAt`; `ratMulCongLeftThm` (ratCanon of the product-pair invariant under swapping the left operand for a cross-equivalent one — cross-eq via mul4Swap + hyp + intOfNumMul, fed to `ratCanonRespects`), `ratMulCongRightThm` (= cong-left between flipped pairs), `repMulEquivAt` (REP(ratMul q r) ≈ unreduced pair). **`ratMulAssocThm`**: both sides → ratCanon of a product-pair on a *reduced* inner product, cong-swap to the *unreduced* one, then **literally equal** (num `intMulAssoc`, denom `timesAssoc` — no cross-equivalence, simpler than ratAddAssoc). **`ratMulDistribThm`** (`ratMul z (ratAdd w v) = ratAdd (ratMul z w) (ratMul z v)`): LHS → `ratCanon(a·(c·F+e·D), b·(d·f))` [mul cong-right]; RHS → `ratCanon((a·c)·&ℤ(b·f)+(a·e)·&ℤ(b·d), (b·d)·(b·f))` [add cong-left+right, swap *both* ratMul reps]; pairs carry a redundant factor b (NOT literal) so closed via `ratCanonRespects` + an explicit int-ring cross-equation (both → COMMON=`(a·(NSUM·B))·G`, NSUM=`c·F+e·D`, B=`&ℤb`, G=`&ℤ(b·(d·f))`; RHS_num factored by `intMulDistrib`/`intRDist`, LHS denom `&ℤ((b·d)(b·f))=&ℤb·&ℤ(b·(d·f))` via a 4-step nat reassoc + `intOfNumMul`). **ℚ is now a commutative ring.** **`ratInv` + `ratMulInvThm` → FIELD**: `ratInv q = ABS_rat(ratCanon(intMul a (&ℤb), |a|·|a|))` — numerator `a·&ℤb` carries sign(a) and magnitude |a|·b, denom |a|²; value sign(a)·b/|a| = 1/q (`ratInv 0` junk by convention — **NO `COND`/sign-fn needed**). `ratMulInvThm` (`¬(q=&ℚ&ℤ0) ⇒ ratMul q (ratInv q) = &ℚ(&ℤ(SUC 0))`): mul cong-right swaps REP(ratInv q) for its unreduced pair; product-pair numerator `a·(a·&ℤb) = (a·a)·&ℤb = &ℤ|a|²·&ℤb = &ℤ(b·|a|²)` (NEW `intSqNatAbsThm`: `z² = &ℤ(|z|·|z|)`, carve case-split — sign drops), so pair = `(&ℤm, m)`, m=`b·|a|²`, which NEW `ratCanonSelfThm` (`¬(m=0) ⇒ ratCanon(&ℤm,m)=(&ℤ1,1)`) reduces to REP(&ℚ&ℤ1). Supporting ℕ/int (proper homes Num/Int.wl, kept in Rat pending migration): `intNatAbsOfNumThm` (`|&ℤn|=n`), `gcdSelfThm` (`gcd m m=m`), `intNatAbsNonzeroThm` (`z≠0 ⇒ |z|≠0`), `ratNumNonzeroThm` (`q≠0 ⇒ FST(REP q)≠&ℤ0`); `repRatInvThm`/`repInvEquivAt` (both carry the `q≠0` hyp). Build pitfalls hit+fixed (both in distrib): an EQMP miss (`notSndZ` needed an `INST` since the first var was `z` not `q`) and a TRANS slip (`aeBD` prepended `eN` not `a`); everything else first-try.

---

## M7-6 `stdlib/Rat.wl` — ℚ order (stage f core DONE)

**M7-6 ℚ IS LINEARLY ORDERED — stage f core (`ratLe`/`ratLt` + linear-order axioms) DONE + COLD-VERIFIED (2026-06-02; cold Strict run_all 1969/0, dev_rat 152/0).** `ratLe q r ⟺ intLe (intMul (FST(REP q)) (&ℤ(SND(REP r)))) (intMul (FST(REP r)) (&ℤ(SND(REP q))))` — cross-multiplication with positive denominators (a/b ≤ c/d ⟺ a·d ≤ c·b since b,d>0), reducing to Int's `intLe` on the cross-products. Unlike Int's own order layer (which runs on ℕ ARITH over FST/SND sums), Rat's order runs on **Int order + intMul ring lemmas** because the cross-products are `intMul` (opaque to ARITH). `ratLt` mirror with `intLt`. Helpers: `ratNumOf`/`ratDenOf` (FST/SND of REP), `ratCrossL`/`ratCrossR` (the two cross-products), `intOfNumNonneg` (`intLe (&ℤ0)(&ℤn)`), `intOfNumNeqZero` (`¬(&ℤn=&ℤ0)` from `¬(n=0)`), `ratDenNeq0` (denominators positive). Proven: `ratLeReflThm` (intLeRefl on `a·&ℤb`), `ratLeAntisymThm` (intLeAntisym → cross-equation = `ratEqCrossThm` backward → `q=r`), `ratLeTotalThm` (intLeTotal + disjunction rewrite), `ratLtNotLeThm` (intLtNotLe at the cross-products). **`ratLeTransThm`** (`∀q r v. ratLe q r ⇒ ratLe r v ⇒ ratLe q v`): the fraction-transitivity argument — multiply `a·&ℤd ≤ c·&ℤb` by `&ℤf` and `c·&ℤf ≤ e·&ℤd` by `&ℤb` (both `intLeMulNonneg`, denominators nonneg), chain through the shared `&ℤf·CB = &ℤb·CF` (int comm/assoc) with `intLeTrans`, reassociate both sides to a common right factor `&ℤd`, then **cancel `&ℤd` (positive)** via the new **`intLeMulNonnegCancelThm`** (`∀u x y. intLe (&ℤ0) u ⇒ ¬(u=&ℤ0) ⇒ intLe (intMul u x)(intMul u y) ⇒ intLe x y`, proper home Int.wl — proved by totality + mul-nonneg + antisym + `intMulCancel`; uses nonneg+nonzero rather than strict `intLt` to avoid an `intOfNumLt` helper). Pitfall hit+fixed: a trailing-underscore pattern var (`notN0_`) left on a helper's RHS made it return garbage → "not a theorem" downstream; dropped the underscore. **NEXT: stage f compatibility (`ratLeAddMonoThm`/`ratLeMulNonnegThm`) — unfold `ratLe` at `ratAdd`/`ratMul` (REP is a ratCanon), swap canon'd cross-product for the unreduced one via `repAddEquivAt`/`ratCanonEquivThm` (a new "ratLe respects cross-equivalence" helper), reduce to int-ring inequality via `intLeMulNonneg`(by `&ℤ(h·h)≥0`) + `intLeAddMono`.**
