# mma-hol — operational notes

Kernel-minimal higher-order logic theorem prover in Wolfram Language, LCF-style, modeled on HOL Light. **stdlib release target (re-scoped 2026-06-13, much narrower): ℝ via Dedekind cuts + sequence theory + closed-interval compactness + connectedness + supporting point-set topology — all in `stdlib/Real/`, single construction, NO calculus. Finish this = declare stdlib complete, publish to GitHub.** Capstones: monotone convergence, Cauchy completeness, Bolzano–Weierstrass, Heine–Borel (closed interval), IVT/connectedness. (Lebesgue-number lemma CUT 2026-06-16 — it was prep for the Riemann/Lebesgue integrability criterion, which is out of scope; no downstream consumer once we stop at real-number theory.) **Why narrowed**: the original goal (validate WL `Module` encapsulation) is already met by the kernel + number tower + automation; the teaching-full-analysis goal moved to the sibling Lean project `tautology`; this lib's other role is checker for the sibling `rum` project, which needs a stable representative stdlib, not深度. Blueprint = `tautology`'s `RealTheory` spine (0-sorry, Lean Init only — see memory `reference-tautology-lean-blueprint`, on disk at `tautology-ref/`). **CUT**: old M8 calculus/Riemann/Lebesgue criterion, M9 multivariable/Stokes, M10 Fourier/Radon; and within RealTheory the spine-orthogonal parts — cardinality/Cantor–Bernstein, decimal expansion/`0.999…=1`, limsup/liminf, the countable-basis/Lindelöf route (Heine–Borel goes via the sup principle + Lebesgue-extension instead). See PLAN §1 + §7 M8.

**Design document: `PLAN.md`** — authoritative for all architectural questions. This file is the session-startup summary only; per-milestone proof detail lives in git commit messages and code comments.

## Environment

- Wolfram Language 14.3 via `wolframscript` 1.13 (Linux). Notebooks are optional front-end only.
- All library code must be script-executable: `wolframscript foo.wls` — exit 0 pass, nonzero fail. Hard requirement.

## Trust boundary (the one thing to never get wrong)

The **kernel** (types + terms + 10 primitive rules + axioms + `new_{constant,definition,basic_type_definition,axiom}`) owns all `thm` value construction. Anything outside is untrusted. Worst-case bug in untrusted code is "can't prove a true theorem"; it must never produce a false theorem.

Hard rules:

0. **All mutable kernel state — `thmTag`, `arityTable`, `constTypeTable`, `axiomList`, `defnList`, `axiomIntakeOpen`, and any future registries — lives in `HOL`Kernel`Private`*`. Read or write outside `Kernel.wl` is forbidden.** Two modes via `Global` $HOLEncapsulationMode`: **`"Strict"`** (default, CI) hides the six state symbols as Module-local gensyms; **`"Stable"`** (dev/persistence) gives them fixed names so `DumpSave` snapshots survive cold boots — there the boundary is convention + CI lint (any `HOL`Kernel`Private`*` reference outside `Kernel.wl` is a bug). Both modes share one `Kernel.wl` body via the `defineKernel[...]` installer; CI runs the full suite in both. Don't add new mutable kernel state outside `HOL`Kernel`Private`*`.
1. **`new_axiom` must disappear after bootstrap.** `ETA_AX`/`SELECT_AX`/`INFINITY_AX` are declared during init, then the API is withdrawn (`lockAxioms[]`). Adding a new axiom is a user decision, not a Claude decision.
2. **Term construction above the kernel goes through `mkVar`/`mkConst`/`mkComb`/`mkAbs`.** Raw `comb[...]`/`abs[...]` literals bypass type-checking and α-normalization — forbidden outside the kernel module.
3. **Kernel is a singleton.** `makeKernel[]` runs once at load; no reset. To clear state, restart `wolframscript`.
4. **Bound variables use a separate head `bvar[k_Integer, ty]`** (de-Bruijn levels-from-binder): inside any `abs`, the immediately enclosing binder is `bvar[0, ty]`, k levels out is `bvar[k, ty]`. The origin name lives in `abs[bv, body, origin]`'s 3rd slot for the printer only. Free vars (`var[name, ty]`) and bound vars are structurally distinct heads. **α-equivalence is `aconv`, not `===`** (`===` distinguishes origin) — kernel rules comparing up to α must use `aconv`.
5. **Printer correctness is independent of soundness.** A printer bug rendering `⊢ False` as `⊢ True` won't break the kernel but deceives readers — printer needs regression tests.

## Conventions

- **工作语言 / Working language**: 与用户交流的首选语言为**中文**，其次为**英文**;禁止使用其它任何语言(包括日语)。代码、标识符、commit message 仍用英文。
- **File extensions**: libraries `.wl` (`BeginPackage`/`EndPackage`); entry points/tests/demos `.wls`; notebooks `.nb` optional.
- **New-milestone file layout (from M8/ℝ on): milestone = folder, not monolith** — see **PLAN §8.1**. Split by the internal dependency tree (each small file = one defs/lemmas closure with explicit exports/imports); centralize term-builder/unfold vocabulary (folder files share one package context; cross-milestone helpers in a shared vocab module) so relocation stays cheap. Optimize for cheap boundary change, not perfect up-front interfaces. Existing Num/Int/Rat/FTA stay monolithic (no retro-split). The 2026-06 Rat→Num/FTA/Int migration that motivated this is the cautionary tale.
- **Contexts**: `HOL`Kernel``, `HOL`Bool``, … Private helpers in `` `Private` `` subcontext. **Private symbols don't cross files** — a helper/value defined in one file's `` `Private` `` (e.g. `numInductBy`, `numTy`, `plusTm` in Num.wl) is unreachable by bare name elsewhere; reference the full `HOL`Pkg`Private`name` path or redefine locally, else it silently stays unevaluated and trips a "not a theorem" / "mkComb type mismatch" far downstream.
- **Errors**: kernel throws `Throw[Failure["HOLError", <|"tag"->…, "msg"->…|>], holErrorTag]`; top-level `Catch` in `.wls` entries. Tests assert on tag. **Never `Message` + `$Failed`.** Read payload via `f[[2, "tag"]]` (not `f[[1, …]]`). **`holError` is HoldRest**: msg/payload must be a literal String/Association AT the call site — a computed expression (`name <> "…"`, a payload variable) matches no DownValue, so nothing throws and the call returns unevaluated (bit Codex brief-005).
- **Tests**: `tests/harness.wl` gives `assertEq`/`assertTrue`/`assertThrows`/`runTests`/`testExit`. Runners: `tests/run_all.wls` (cold-boot Strict, CI, ~10-15 min); `tests/run_all_stable.wls` (cold-boot Stable, CI sanity); `tests/run_fast.wls [pattern…]` (restore `bootstrap.mx` snapshot, run matching files, ~3 s). `kernel_tests.wl` is skipped from the fast runner.
- **Snapshot dev loop (the frontier workflow — PLAN §8.1.6, built 2026-06-04).** `bootstrap.mx` = the current *frozen base* = exactly the files in `build_snapshot.wls`'s load list. While developing a NEW file, keep it OUT of that list and iterate with **`tests/dev.wls <frontier.wl> [pat…]`** — restores the base + loads ONLY the frontier file on top + runs matching tests (~0.1 s vs a ~7-min cold rebuild; staleness-checks all sources EXCEPT the named frontier files). When the file graduates: add it to the three runners' lists (below), then **`tests/extend_snapshot.wls <file.wl>`** folds it into `bootstrap.mx` incrementally (~0.2 s, NOT a cold rebuild). Full **`tests/build_snapshot.wls`** (optional `<uptoPattern>` = partial build through a layer; prints per-file timing) is the cold re-baseline — needed only when an UPSTREAM file changes or to produce a clean authoritative base. dev/extend are Stable-mode like run_fast (catch logic errors, NOT Strict gensym/load-order) → the per-stage authoritative gate stays the cold Strict `run_all`; re-baseline with full `build_snapshot` periodically (extended snapshots can drift a few KB from cold).
- **Adding a module**: add it to the load lists in all three runners — `tests/run_all.wls`, `tests/run_all_stable.wls`, and `tests/build_snapshot.wls` (dependency order). Forgetting `run_all_stable.wls` is how the Stable suite silently drifts out of sync with Strict. (`dev.wls`/`extend_snapshot.wls` need no list — they operate on the snapshot incrementally.) Test files use `Needs["HOL`…`"]` which is a no-op once loaded but can't locate flat-layout files on its own.
- **No comments describing what code does** — names carry that. Comments only for non-obvious *why*.
- **No `_` in identifiers**: WL parses `FOO_BAR` as `Pattern[FOO, Blank[BAR]]`, silently introducing separate symbols that can shadow imports. Applies to Module locals too (`x_eq` → `xEq`). Tactics camelCase (`conjTac`, `asmSimp`); combinators ALL-CAPS underscore-free (`THEN`, `REPEAT`, `SIMP`, `MESON`).
- **No `Function[{_}, body]`**: `_` is not a valid Function parameter; WL emits `Function::flpar`, leaves the call unevaluated, and garbage propagates into kernel functions. Use a real symbol.
- **WL comments close at the first `*)`** — `(a*)`, `(SUC k*)` etc. in comment prose silently terminate the comment; error points far below the cause.
- **Variable-capture hygiene.** A HOL variable's identity is **(name string, type)**, NOT the WL symbol holding it — `Module` seals WL symbols (`qV`/`qBig`) but NOT the `mkVar` *name* (`mkVar["q",…]` in two places = one HOL variable; this caused the `nnMulCutIsCut` `ABS: binder occurs free in hypotheses`). Two **locally-bounded** rules (never "scan all callers"):
  - **(a) Extract, don't rebuild.** A subterm that must α-match kernel output (β-reduced ∃-body, the `IS_CUT` 4-conjunction, …) → take it from the actual theorem (`concl[…][[…]]`), don't hand-reconstruct. The kernel α-renames binders when the substituted term shares their names; a hand-built term won't, and `EQMP`/`EXISTS` reject it on `aconv`. (The `realNnMul` open-case bug: midpoint `½(q+p·q)` contained `p,q`, so a rebuilt `∃p∃q` captured them.)
  - **(b) Distinctive internal binders.** Any term-accepting helper's internal scaffolding binder, and per-branch witnesses in multi-case proofs, get a distinctive name (`funcExtThm`→`xFE`, `negExTerm`→`rNE`) — never a bare common name (`x/q/r/p/s`) a caller's term might carry. Bare short names only for a proof's single widest-scope role + final-statement canonical binders (those are `aconv`/capture-avoiding-substitution protected). The binder-vs-arg **type** difference (real arg / rat binder) often saves you — don't rely on it. **`freshName`/`collectFreeNames` are Bool-private ⇒ unusable in stdlib; use the distinctive-name discipline there.**
- **Resource cost vs convenience**: on hot paths (anything per-node in proof traversal — `freesIn`, `vsubst`, `instType`, `betaSubst`, `tyvarsInTerm`, `aconv`) prefer the costlier-to-write but cheaper-to-run alternative. (Worked precedent: structural `bvar[k, ty]` head over name-encoded indices — ~2× faster per traversal at the price of a `bvar` clause in every term walker.)
- **Debug with narrow probes, never whole-term dumps.** When a kernel rule fails mid-proof, do NOT `Print` the full term tree (`Print[concl[th]]` / `Print[…Short[#,5]&]` on a deep term) — one such dump is thousands of chars, and every later turn re-pays for it in context. Instead probe narrowly: `Head[th]` (is it a `thmTag` or an unevaluated call?), `hyp[th]`, `MatchQ[concl[th], <small pattern>]`, or print a single sub-slot (`concl[th][[1,2]]`). Binary-search the failing step by moving a single shape check, not by dumping everything. Token cost is dominated by accumulated context, so a few KB of term-tree spam per build round compounds fast. (Snapshot rebuilds are wall-clock-heavy but token-cheap; the expensive thing is large tool output.)

## Directory layout (see PLAN §8 for the full tree)

```
Kernel.wl  Basics.wl  Bool.wl  Equal.wl  Drule.wl  Tactics.wl  Parser.wl  Printer.wl
auto/        — MESON / SIMP / SET / ARITH / REAL_ARITH
stdlib/      — Pair, Sum, Option, Set, Num, Int, Rat, Real, List, Finite, (Complex)
stdlib/Real/ — M7 ℝ construction + M8 Seq.wl/Compact.wl/Connected.wl/Topology.wl  [stdlib release target]
             (analysis1/2/3 — old calculus→Stokes→Fourier — CUT 2026-06-13)
tests/  demos/
```

## Milestones (PLAN §7 has the detail)

M1 Types · M2 Terms · M3 Kernel (10 rules + bootstrap) · M4 Derived rules · M5 Tactics · M6 Parser/Printer · M7 stdlib + 5 automation tactics (`MESON`/`SIMP`/`SET`/`ARITH`/`REAL_ARITH`) + ℝ-complete-ordered-field · **M8 ℝ sequences + compactness + connectedness + topology [stdlib release target; `stdlib/Real/`, Dedekind only, NO calculus]**. M9 multivariable/Stokes + M10 Fourier/Radon — CUT 2026-06-13.

Capstones: M3 `⊢ T`; M7 `dedekindCompleteThm` (sup principle); **M8 set — monotone convergence + Cauchy completeness + Bolzano–Weierstrass + Heine–Borel + IVT (stdlib release capstones; Lebesgue-number lemma CUT 2026-06-16)**.

## Git

- Branch `main`. Local config: `Si-Qi Liu <liusq@tsinghua.edu.cn>`.
- Commit only when asked. Prefer new commits over `--amend`. Don't touch global git config.

## Current state

M1–M6 done (kernel, derived rules, tactics, parser+printer; **M6b notebook front-end CUT 2026-05**).

**M7 automation** — `MESON`/`SIMP`/`asmSimp`/`SET` substantially complete. `ARITH` (auto/Arith.wl) does **linear ℕ (∀/∃/= + atom abstraction)** via a Fourier–Motzkin oracle + kernel verifier (Farkas certificates, ℕ-native — NOT internal Cooper QE; Cooper cut 2026-05-28, see memory `project_arith_oracle_verifier`). **REAL_ARITH (M7-ε) COMPLETE 2026-06-11** — `auto/RealArith.wl` (`realArithProve` + `REALARITH[]` tactic; the WL symbol has NO underscore — `_` is not a valid WL symbol character; Codex briefs 005–007, see module map). MESON Brand-modulation + NOT_FORALL/NOT_EXISTS NNF deferred till a goal forces them.

**M7 stdlib** — Pair, Sum, Option, Set, Num, List, Finite, FTA done. **`stdlib/Int.wl` COMPLETE** — ordered integral domain: `&ℤ` embedding; `intNeg`/`intSucc`/`intPred`; `intAdd`/`intMul` (comm·assoc·distrib·identity); no-zero-divisors `intMulEqZeroThm` + cancellation `intMulCancelThm`; order `intLe`/`intLt` (refl/antisym/trans/total + add/neg/mul-nonneg monotonicity); `intAbs`; bidirectional induction `intInductionThm`. Engine: Grothendieck-equivalence `canon{Equiv,Inj,Respects}` + `repInt{Add,Mul}`, ARITH discharging FST/SND-atom num glue. **`stdlib/Rat.wl` — ℚ IS A DENSELY-ORDERED FIELD (M7-6 COMPLETE, stages a–g)** (cold Strict run_all 1994/0; `run_fast rat_tests` 177/0): reduced fractions over `int×num`, kernel `=` IS rational equality (no setoid); additive abelian group (`ratAdd` comm/assoc/zero + `ratNeg`) + commutative ring (`ratMul` comm/one/zero/assoc/distrib) + `ratInv`/`ratMulInvThm`; lowest-terms uniqueness `ratEqCrossThm`. **Order** `ratLe`/`ratLt` via cross-multiplication (reduces to Int's `intLe` on cross-products): linear-order axioms + compatibility `ratLeAddMonoThm`/`ratLeMulNonnegThm` (Int helpers `intLeMulNonnegCancelThm` + `pairLeCong{Left,Right}Thm`); strict-order `ratLtAddMonoThm`/`ratLtMulPosCancelThm` + `ratAddSubCancelThm`. **`&ℚ` ring/order homomorphism** `ratOfInt{Add,Mul,Le}Thm` + **density** `ratDenseThm` (`q<r ⇒ q<½(q+r)<r`, midpoint via `ratMulTwoThm` `x·2=x+x` + `ratMulInv`). Engine: **ratCanon-respects + add/mul cong tower** + the pairLeCong order tower. **Carried ℕ/int lemma migration DONE 2026-06** (cold Strict run_all 1994/0): ℕ gcd/divisibility/Bezout/coprime theory + `exDivSelf` → FTA.wl, `exDiv`+`multNonzero` → Num.wl, `intNatAbs`/`intDivNat` magnitude layer + `intLeMulNonnegCancel`/`pairLeCong{Left,Right}` → Int.wl. Rat no longer carries any ℕ/int lemma. **Boundary consolidation DONE 2026-06 (user decision: heavy divisibility → FTA):** `gcd` (def + universal property), `prime`, `euclidLemmaThm` also moved Num→FTA; Num keeps only the `divides` predicate + basic arith + DIV/MOD + exDiv as ℕ-foundational. FTA is now the single home for ℕ divisibility theory.

**NEXT TASK (fresh session resumes here): `stdlib/Real/Seq.wl` M7-8 Stage 2+ — Stage 1 DONE 2026-06-13 (Codex brief-008, ZERO-debug, cold Strict run_all 2470/0, commit 84e174b): `tendsto` (REAL ε, relation-first — user decisions) + `convergent` defs, tendstoConst/Unique/Add/Neg/Sub, `realNeAbsPosThm`; FIRST REAL_ARITH consumer (7 ε-glue facts = one-line load-time `realArithProve` calls). Seq.wl is FRONTIER (in run_all/run_all_stable AFTER auto/RealArith.wl — load-time realArithProve dependency; NOT in build_snapshot.wls; dev loop `tests/dev.wls stdlib/Real/Seq.wl real_seq`). Stage 2 DONE 2026-06-13 (briefs 009 `eventually` combinator + convergent⇒bounded + away-from-zero, ZERO-debug; 010 abs-mul + product + scalar laws, one SYM-orientation fix; cold Strict run_all 2526/0; commits 1a67e17 + e04cbdc). Stage 3 DONE (brief-011, monotone convergence from `dedekindCompleteThm`, cold Strict 2568/0, commit 00981cf). Stage 4 DONE (brief-012, **subsequence INFRASTRUCTURE only** — Peak/rising-index num→num recursions via the iteration principle + `existsMonoSubseqThm` "every sequence has a monotone subsequence"; three Claude fixes incl. the deep-beta-unfold bug, see [[wl-unfold-deep-beta-select]]; cold Strict 2598/0, commit 8d27504). Stage 5 DONE (brief-014, Cauchy ⟹ convergent via the eventual-lower-bound sup `FromSupCauchy`, M7 bridge capstone `⊢ ∀a. Cauchy a ⇒ ∃L. tendsto a L`, commit 58455bf; FIRST full-access self-verify brief). Stage 6 DONE (brief-015, nested-interval principle from the sup `FromSupNested`, commit e20f696). **SEQUENCE LAYER (M8.1) COMPLETE — `Seq.wl` GRADUATED into bootstrap.mx 2026-06-14 (cold rebuild, 1162 KB, cold Strict run_all 2658/0, commit c5de53f).** **M8.2 COMPACTNESS DONE + GRADUATED 2026-06-16** (two independent branches, owner re-plan, Lindelöf-free; PLAN §7 M8.2). `stdlib/Real/SeqAux.wl` (Dyadic + nested helpers) + `stdlib/Real/Compact.wl` (the compactness theorems) FOLDED INTO `bootstrap.mx` (cold rebuild; no longer frontier — `run_fast real_compact real_seqaux` works; new Real files like Connected.wl use `dev.wls stdlib/Real/Connected.wl real_connected`). **Branch A DONE**: brief-016 BW列紧 (`bwSequentialThm` = existsMonoSubseq + monoConverges, commit c171e29), brief-017 聚点 (`accumulationPrincipleThm` via freshList num→list recursion + SeqToAccum, commit b337ff0). **Branch B DONE** (区间套✅ Stage6 → Heine–Borel `FromNestedFinite`; Lebesgue CUT): briefs 018 Dyadic (`dyadicArchThm`, bc3d0e2) / 019 nested helpers (527b0a9) / 020 cover vocab (`openInterval`/`closedInterval`/`isOpen`/`covers`/`listSubcover`/`finiteSubcover`, polymorphic iota, 183353d) / 021 subcover algebra (`midpoint`/`combineHalfSubcover`/`rightHalfBad`, 06d9a16) / 022 bisection recursion (`bisectInterval` num→real×real pair recursion + `lower`/`upper`/`badIntervals`/`intervalOrder`/`lowerMono`/`upperAntitone`/`nestedIntervals` — THE hardest, 3c67f05) / **023 区间长度→0** (`length` + `lengthsToZeroThm` via `dyadicArchThm`, 8eca9cd) / **024 Heine–Borel finish** (`intervalSubsetOpenInterval` + `singletonSubcoverOfSmallInterval` + 反证 → **`compactnessPrincipleThm`**, 60c6a19). **Cold Strict run_all 2906/0.** brief-024 NOTE: Codex FALSELY claimed green — 3 Claude fixes at acceptance (`HOL\`Stdlib\`Num\`plusTm` is PRIVATE→build via `plusConst`; `arithProve` can't do `a≤a+b`→`leqDefThm`+EXISTS; `unfoldTendsto` β-redex vs `seqLengthCloseAll` β-normal broke a CHOOSE→`compactBetaClean` the ∃-thm). **Lebesgue (brief-025/`FiniteToLebesgue`) CUT 2026-06-16** (owner: it was prep for the Riemann/Lebesgue integrability criterion; no downstream consumer once we stop at real-number theory; blueprint also needs a `LocalIndex` record type we'd have to subtype-carve). **SeqAux+Compact GRADUATED into bootstrap.mx 2026-06-16. M8.3 CONNECTEDNESS DONE 2026-06-16** (`stdlib/Real/Connected.wl`, FRONTIER not yet graduated; briefs 026 vocab + `intervalSet_*` / 027 `trace`+`openInTrace`+`connected⇒interval` / 028 sup-argument `notSeparationOfIntervalOrderedPoints` + **`connectedIffIntervalSetThm`** (connected ⟺ interval) + IVT + 8 `connected_*` corollaries; cold run_all 3014/0; commits 244d481/e51adbb/b039a08/ce82f34). brief-028 Codex got the whole capstone incl. the hard sup lemma, dev.wls claim reproduced (108/0, no false green). **ROUTE (owner 2026-06-16): 列紧 ⟺ 有界闭 ⟺ 紧** via closed+bounded bridge (Lindelöf-free; the M8.2 two-branch cut is RECONNECTED). **M8.4 `Topology.wl` DONE 2026-06-16** (FRONTIER, not yet graduated; brief-029 `compl`/`isClosed`=isOpen-of-compl/`relativeClosed`/`closedIn`/`closedInSubset`/`closedIntervalIsClosed`, commit 70b88a2 / brief-030 `pointInOpenIntervalOfTendsto`+`limitMemOfClosed` (closed set contains seq-limits), commit 281d774; cold run_all 3045/0; SKIPed closure/interior/Lindelof). **M8.5 `CompactSet.wl` SEQUENTIAL SIDE DONE** (FRONTIER, not yet graduated): brief-031 `isSequentiallyCompact` def + `seqBoundedOfSetBounded` + `sequentiallyCompactOfClosedBounded` (有界闭⇒列紧 via BW+limitMemOfClosed), ee8a537 / brief-032 `boundedOfSequentiallyCompact` (列紧⇒有界, escape sequence + realArch; `natRealLe`/`existsOutside`/`ltAbsOfOutside`/`unboundedEscapePoint`), 0943d95 / brief-033 `seqTendstoSubsequence` + `invSuccRadius`(=1/(n+1)) pos/antitone/**`invSuccRadiusTendstoZero`** (1/(n+1)→0, the analytic prereqs), e75f4a0 / brief-034 列紧⇒闭 `closedOfSequentiallyCompactThm` (via the `nearClosedPoint` ε-sequence `@y. S y ∧ openInterval (x−1/(n+1)) (x+1/(n+1)) y` + `seqTendstoSubsequence`+`tendstoUnique`; helper `existsMemIntervalOfNoComplNeighborhood`) + **`sequentialCompactIffClosedBoundedThm`** (列紧 = isClosed ∧ setBounded, 3-direction packaging via `DEDUCTANTISYM`), b3753be; **cold run_all 3091/0** (= 3079 + 12 asserts). (`setBounded`/`seqBounded`/`bwSequentialThm`/`hasConvergentSubseq` reused from the graduated Compact.wl.) **SEQUENTIAL SIDE COMPLETE 2026-06-17 — 列紧⟺有界闭 (3 directions all proven; Codex brief-034 真green, csetNeighGoal one-shot aconv).** **NEXT = open-cover side**: `isCompact` needs a SET-OF-SETS cover encoding `C:(real→bool)→bool` (the blueprint's `∀{ι}` TYPE quantification has NO HOL analogue — Claude to design; instantiate the polymorphic `compactnessPrincipleThm` at ι:=real→bool) → `compact_iff_closed_bounded` (HeineBorel.lean centerList, brief-024 family) → `compact_iff_sequentialCompact` corollary. Blueprint `RealCompactness/{SequentialCompactness,HeineBorel,CompactSequential}.lean`. Then graduate Connected/Topology/CompactSet, declare stdlib complete, publish. PLAN §7 M8.4/M8.5. **ACCEPTANCE DISCIPLINE: cold gate's `passed:` must be ≥ prev baseline + new asserts (not just `failed:0`) — a stray `testExit[]` in a test file silently truncates run_all (bit brief-016; background gate may spuriously report exit 1, re-run foreground with `echo $?`).** **ROUTE DISCIPLINE: before each brief read `tautology-ref/Tautology/RealBasic/ValveRoom.lean` active route + Routes chain. See [[reference-tautology-lean-blueprint]].** Borrow blueprints via [[reference-tautology-lean-blueprint]] (tautology-ref/, gitignored). **Codex now self-verifies: dispatch `-s danger-full-access`, briefs require iterate-to-green via dev.wls (see [[workflow-codex-delegation]] + TEMPLATE Verification).** M7-ε REAL_ARITH COMPLETE 2026-06-11 (`auto/RealArith.wl` via Codex briefs 005–007, capstone `∀a b. a<b ⇒ a<(a+b)/2` proven, `REALARITH[]` tactic, GRADUATED into bootstrap.mx — see "M7 automation" + module map + PROGRESS.md) — current suite: cold Strict run_all 2432/0.** ℝ IS A COMPLETE ORDERED FIELD (Complete.wl DONE 2026-06-10); M8-prep order toolkit `Real/Abs.wl` (|·| + triangle ineq) + `Real/MinMax.wl` (max/min + |x|=max x (−x)) DONE 2026-06-11 via the Codex delegation workflow (see "Codex delegation workflow" below; briefs 001–004 also closed TODO.md F+G), both GRADUATED into bootstrap.mx — current suite: cold Strict run_all 2306/0.** **`Real/Complete.wl` DONE 2026-06-10 — Dedekind completeness + Archimedean + ℚ-density (zero-debug first pass; tests `tests/real_complete_tests.wl` 30/0):** `realSup S = ABS_real (λq. ∃a. S a ∧ REP_real a q)` (UNION of member lower sets, Lean blueprint Cut/Sup.lean); `supCutIsCutThm` (`(∃a. S a) ⇒ (∃u. ∀a. S a ⇒ a≤u) ⇒ IS_CUT …`) + conditional `repRealSupThm`/`realSupMemThm`; `realSupUpperThm`/`realSupLeastThm`/packaged **`dedekindCompleteThm`**; `realRatBoundThm` (`∀x.∃q. x<&ℝq`, properness point bumped by 1) → **`realArchThm`** (`∀x.∃n. x<&ℝ(&ℚ(&ℤn))` via `ratArchThm` + strict trans); **`realDenseThm`** (`x<y ⇒ ∃q. x<&ℝq<y`: separating point from `notLeWitnessThm`, ONE openness bump, right side killed by `ratLtIrrefl` — no third openness needed). Also added the real strict-order vocabulary `realLtImpLeThm`/`realLtLeTransThm`/`realLeLtTransThm`/`realLtTransThm` + strict-order embedding `realOfRatLtThm`. Key new idiom: `chooseBody[exTm,w]` extracts the CHOOSE hypothesis instance from a kernel BETACONV (extract-don't-rebuild applied to ∃-elimination). **ℝ ORDERED FIELD DONE + GRADUATED 2026-06-10 (cold Strict run_all 2220/0):** Cut + Field (group) + Mul (ring + signed distrib + ×/≤ compat) + Inv (field) + Stage E (`&ℝ` ring/order homomorphism `realOfRat{Add,Neg,NnMul,Mul}Thm`; `realOfRatLeThm` in Field) — all of `stdlib/Real/{Cut,RatAux,Field,Mul,Inv}.wl` now FOLDED INTO `bootstrap.mx` (no longer frontier; `run_fast real` and `dev.wls <newRealFile> real` both work). **Ordered-field ×/≤ compatibility DONE 2026-06-10 (cold Strict run_all 2212/0):** additive-order `realLe/LtAddMonoThm` + bridges `realLeSubNonnegThm` (`a≤b⟺0≤b−a`)/`realLtSubPosThm`, multiplicative-order `realLeMulMonoThm`/`realLtMulMonoThm` (all in Mul.wl's ordered-field-surface tail). **`realInv` DONE 2026-06-09 (cold Strict run_all 2200/0) — ℝ IS A FIELD** (`Real/Inv.wl`: `invPos` Rudin reciprocal core + `invPosMulThm` `0<x⇒x·(1/x)=1` + signed `realInv` + `realMulInvThm` `¬(x=0)⇒x·(1/x)=1`; user-chosen order was inverse-before-compat). ℝ is being built in a **`stdlib/Real/` FOLDER** per PLAN §8.1 (milestone=folder; files share context `HOL`Stdlib`Real`; vocabulary centralized). **`Real/Cut.wl` COMPLETE + cold-verified** (carve + full linear order). **`Real/Field.wl` Stages A+B+C COMPLETE + cold-Strict-verified (2026-06-05, run_all 2125/0; dev.wls real 131/0):** **A** `&ℝ:rat→real` embedding (`realOfRat`/`repRealOfRat`/`realOfRatMem`/`realOfRatInj`/`realOfRatLe` order embedding); **B** `realAdd` (cut sum `{r:∃s. s∈L_x ∧ (r−s)∈L_y}`: `sumCutIsCut` + `realAddMem` + comm/assoc/zero) — additive monoid; **C** `realNeg` (Rudin `{p:∃r. 0<r ∧ −(p+r)∉L_x}`: `negCutIsCut`) + **`realAddNegThm` `x+(−x)=0`** ⟹ **ℝ is an additive abelian GROUP**. The inverse was the hardest theorem: translated from a sibling Lean Dedekind dev (`../tautology` Cut/Add.lean+Arch.lean) — built ℚ-Archimedean from ℤ (`intArch`→`ratArch`, they used `Rat.ceil`), mul-order layer (`ratMulRightCancel`/`ratLtMulPos`/`ratNatMulGt`), `cutStraddle` (numInduction), easy+hard halves. Many parked ℚ/ℤ lemmas now live in Field.wl (Archimedean, mul-order, negation algebra) — **extract into a new `Real/RatAux.wl` (loaded first in the folder) as the pre-mul cleanup commit** (§8.1 within-folder move = free; `Mul.wl` consumes it). (Historical: Field.wl was a FRONTIER file until the 2026-06-10 graduation — the whole Real folder is now IN `bootstrap.mx`, so `run_fast real` works and new Real files use `dev.wls <newfile> real`.) **Stage D `realMul` COMPLETE — Layers 1 (non-negative core) + 2 (signed) done; cold Strict run_all 2181/0 (2026-06-09).** Step 0: `Real/RatAux.wl` extracted (ALL `rat*`/`int*` ℚ/ℤ aux; Field 1669→1099) + `Real/Mul.wl` created + runners wired **Cut→RatAux→Field→Mul** (then frontier; ALL GRADUATED into the snapshot 2026-06-10). **Layer 1 DONE:** `realNnMul` (cut body STRICT `r<p·q` ∨ `r<0`) + `nnMulCutIsCutThm` (4 conds — proper drops Lean's COND clamp because `0≤x` ⟹ every non-member ≥0; open is pure `ratDense`) + rep/mem; **extraction-based membership helpers** `nnMemEq`/`nnMemIntroL`/`nnMemIntroR`/`nnMemPosThm` (positive ⟹ product-witness ∃)/`nnMemProdThm` (s∈a,t∈b ⟹ s·t∈a·b — STRICT `<` needs an openness bump on each factor) — all EXTRACT ∃-terms from the kernel, never rebuild (capture-proof, per the hygiene rules); algebra `realNnMul{Comm,Nonneg,Zero,One,AssocNonneg}Thm`. RatAux extras (One/Assoc): `ratLeLtTrans`/`ratMulPos`/`ratInvPos`/`ratLtImpLe`. **`realNnMulDistribNonnegThm` DONE (2026-06-09, last Layer 1 piece; cold Strict run_all 2148/0):** `0≤x∧0≤y∧0≤z ⇒ x·(y+z)=x·y+x·z`; added `ratLtLeTransThm` (RatAux) + private `realAddNonnegThm` (`0≤y∧0≤z⇒0≤y+z`) + endpoint-extracting ℚ micro-helpers `ltLt2`/`ltLe2`/`leLt2`/`ltAddR`/`ltMulR`/`rwLt`/`notPosToLe0`/`notNegToGe0`/`posToNe0` (cut SPEC bookkeeping) + `distribAux` (Rudin: build a NEGATIVE `g∈cut` with `0<g+M ∧ r<p·(g+M)`, by_cases `r<0`). **⊆ ("fwd"): ONE density split (`r<p·t+p·w`) + per-summand `0<t?`/`0<w?` EXCLUDEDMIDDLE — STRICT `<` means the boundary `p·t` is NOT in the cut, so density not Lean's reflexive-boundary; this unifies Lean's 3 sign-cases into 2 independent membership tests.** ⊇ ("bwd"): the Lean 4-case tree (both-neg → `r<0`; mixed → `distribAux` on the OTHER factor; both-pos → `exists_gt_both` via `ratLeTotal` + open-bump). Gotcha solved: bwd must `CHOOSE` the realAdd witness `s` to discharge it, else `s,r` leak into hyps and `GEN[r]`'s ABS throws "binder occurs free in hypotheses". **Layer 2 (signed `realMul`) DONE (2026-06-09, cold Strict run_all 2181/0):** def = nested `COND` binary `0≤x`/`0≤y` (zero folds into nonneg — NOT ternary) + 4 case-reduction lemmas `realMulCase{PP,PN,NP,NN}Thm` + a `splitSign` combinator (the EM+DISJCASES case framework, reused everywhere — chosen over a fixed-arity `realMulSignCases` because realMul subterms differ per theorem); **realNeg sign algebra** `realNegZero`/`realNegNeg`/`realLeNeg`(取负反序)/`realNegAdd`/`realNegNonneg`/`realNegNonpos`/`notNonnegNeg` + `realAddCancelLeft`; **sign homomorphism** `realMulNegRight`/`realMulNegLeft` (pull all signs out — THE reduction tool); `realMul{Zero,One,Comm}` (clean binary, no product-sign); `realMulAssoc` (nested 逐符号剥离 recursion to the all-nonneg base `assocCore`, each negative factor pulled via `mulNeg` — binary 2³ not Lean's ternary 27); **`realMulDistribThm`** = the no-reference hardest step (core `distribNonnegX` for `0≤x` splits y,z,(y+z) reusing `realNnMulDistribNonneg`+real add group via `mixedCase`/`nnNegCase`, then `x<0` peeled by `mulNegLeft`+`realNegAdd`); order surface `realMulNonneg`=`realLeMulNonneg` + `realLtMulPos` (+ `notLeWitness` 非包含⟹见证 + `realPosHasPosMem` 正实数有正成员). **GOTCHA:** `HOL\`Auto\`PropTaut\`eqfIntro` (¬p⊢p=F) is PROVATE ⇒ 跨文件返回未求值 → 触发 `MKCOMB: arguments must be theorems`; rebuilt `eqfIntroL` locally (per the Bool-private-helper rule). 3-layer design (full detail + Lean lessons → PLAN Phase-3 M7-7 "Stage D realMul 架构"): **(1)** nonneg core `realNnMul`, cut body with STRICT `r < p·q` (not Lean's `≤` — strict makes openness pure density, no `no_max` bump), all semantic thms carry `0≤x ∧ 0≤y` + `…Nonneg` names (unconditional assoc/distrib are FALSE off the nonneg domain); **(2)** signed `realMul` via `COND` on `0≤x`/`0≤y` **binary** split (zero folds into nonneg — NOT zero/pos/neg ternary) + a `realMulSignCases` theorem-builder so comm/assoc/distrib don't each hand-write the case tree; **(3)** abstract ordered-field surface (`realMul{Comm,Assoc,One,Zero,Distrib}Thm`, `realLeMulNonnegThm`, `realLtMulPosThm`) — REAL_ARITH/downstream consume THIS, never `REP_real`. **Lean blueprint (old "Mul.lean BROKEN" = correct point-in-time snapshot; mul was written later):** `../archive/tautology` Mul/MulComm/MulDistrib/Sup now have NO sorry — nonneg core + signed comm/one/assoc + **nonneg** distributivity + Dedekind completeness all proven; the ONLY gap = **signed** distributivity (Lean stops at nonneg) = our single no-reference step (deliverable must include full `realMulDistribThm`). **Why that project was abandoned = the real lesson:** mul *was* provable, but a ternary (zero/pos/neg) plan + no sign-case helper + all-inlined rational algebra made the late-stage workload/token cost explode past budget. Their ternary→27-case `mul_assoc` is the cautionary tale our binary split + `realMulSignCases` + pre-extracted `RatAux.wl` directly hedge against. (Stage E + graduation + Complete.wl since DONE — see top of this block.) `Roots.wl` (nth roots, √2) can follow REAL_ARITH. Side-quests OFF M8 critical path: ℝ_Cauchy+`ℝ_D≅ℝ_C`, decimal `0.999…=1`. Order: Real主体 ✅ → **REAL_ARITH (M7-ε)** → Seq → Cauchy → Decimal. Full plan: PLAN Phase-3 + memory `project_real_construction`.

**Live gaps:** ARITH `arithProve` rejects `¬(=)` hyps (`toLeqFact` throw) — convert via `ltZeroNotZeroThm` to `0<·` first. Audit issues in `TODO.md`.

**Codex delegation workflow (built 2026-06-11, `codex/` dir):** mechanical/spec-complete tasks can be outsourced to the user's Codex CLI (proxy wrapper `cdx`; non-interactive `cdx exec -C <repo> -s workspace-write "Read codex/brief-NNN-*.md and execute it"`). Write a brief from `codex/TEMPLATE.md` (scope + trust-boundary no-touch list + the 10 WL pitfalls + graded delivery + stop-loss); Codex's sandbox CANNOT run wolframscript, so it delivers statically-checked code and ALL verification stays here: git-diff scope check → dev.wls (frontier) or cold Strict run_all (library) → commit. Track record 2026-06-11: briefs 001 (CHOOSE freshness), 002 (ABS retry; one Return-in-For fix needed), 003 (Abs.wl, 205-line proof file, zero-debug), 004 (MinMax.wl + realAbsMaxThm, zero-debug), 005–007 (ALL of auto/RealArith.wl ~1600 lines — ground layer, normalization convs, FM oracle + Farkas verifier + tactic; 006 zero-debug, 005/007 one small fix each: holError-HoldRest call shape, `Symbol["REAL_ARITH"]` invalid-name alias). Proof-class AND decision-procedure outsourcing WORK when the brief pins statement list + proof sketches + available-lemma names + canonical-form contracts.

**Detailed per-stage proof history → `PROGRESS.md`** (moved out of this file 2026-06-02 to keep it lean). Authoritative detail: `git log` + code comments. Design rationale: `PLAN.md`.

## Module map (public role + key exports; internals in the source)

- `Types.wl` (M1): type layer — `tyVar`/`tyApp`, `mkVarType`, `tyvars`, `typeSubst`. No state.
- `Terms.wl` (M2): term layer — `var`/`bvar`/`const`/`comb`/`abs`, `mkVar`/`mkComb`/`mkAbs` (α-canonical), `typeOf`/`freesIn`/`vsubst`/`instType`/`aconv`/`stripOrigin`. No state.
- `Kernel.wl` (M3a+b): trust boundary — 10 primitive rules (`REFL` `TRANS` `MKCOMB` `ABS` `BETA` `ASSUME` `EQMP` `DEDUCTANTISYM` `INST` `INSTTYPE`), `mkType`/`mkConst`/`mkEq`, the 4 extension points + `lockAxioms`, accessors `destThm`/`hyp`/`concl`/`isThm`. `=` pre-registered as `α→α→bool`.
- `Bootstrap.wl` (M3c): logical constants `T ∀ ∧ ⇒ ∃ F ¬` (newDefinition), `@` (newConstant), `ONE_ONE`/`ONTO`; axioms `etaAx`/`selectAx`/`infinityAx`; then `lockAxioms[]`.
- `Equal.wl` (M4a): `SYM`, `APTERM`, `APTHM`, `BETACONV` (β-reduces any redex).
- `Bool.wl` (M4b): `TRUTH`, `EQTINTRO`/`EQTELIM`, `GEN`/`SPEC`/`ISPEC`, `CONJ`/`CONJUNCT1`/`CONJUNCT2`, `MP`/`DISCH`/`UNDISCH`, `NOTINTRO`/`NOTELIM`/`CONTR`, `EXISTS`/`CHOOSE`, `DISJ1`/`DISJ2`/`DISJCASES`, `EXCLUDEDMIDDLE`/`CCONTR`, `freshName`; `COND` (`condConst`/`condDefThm`/`condTThm`/`condFThm`).
- `Drule.wl` (M4c+d): conversion combinators (`ALLCONV`/`NOCONV`/`THENC`/`ORELSEC`/`TRYCONV`/`REPEATC`/`SUBCONV`/`DEPTHCONV`), Miller-pattern `REWRCONV`, rule lifters `CONVRULE`/`ONCEREWRITERULE`/`REWRITERULE`, `SUBS`.
- `Tactics.wl` (M5): `goal[asms,concl]`, `tacResult`; basic tactics (`allTac`/`conjTac`/`disj1Tac`/`disj2Tac`/`genTac`/`existsTac`/`dischTac`/`assumeTac`/`acceptTac`/`popAssum`/`rewriteTac`); `THEN`/`THENL`/`ORELSE`/`REPEAT`/`TRY`; `prove[tm,tac]`; `makeGoalstack[]`.
- `Printer.wl` (M6a): pretty printer + operator registry (package-private Assoc, not in kernel). `formatTerm`/`formatThm` in Unicode/ASCII.
- `Parser.wl` (M6c): string → term/type, `parseTerm`/`parseType`. Tokenizer + W-algorithm inference + unify. **No `ToExpression`** (trust boundary intact).
- `auto/PropTaut.wl`: `propTaut[t]` propositional decision procedure; `nnfThm`/`cnfThm`/`splitConjThm`/`clausifyPropThm`/`clausifyContrapositives` (thm-tracked clausifier for MESON).
- `auto/Meson.wl`: `MESON[thms]` tactic + `mesonProve` — `mLit`/`mClause` rep, NNF/Skolem/CNF preprocessing, Robinson MGU (bool vars rigid), iterative-deepening connection-tableaux search + proof-tree replay; equality lemmas `eqRefl/Sym/TransThm` user-supplied.
- `auto/Simp.wl`: `SIMP[thms]`/`asmSimp[thms]` tactics + `simpConv`/`simpProve`. Equation + conditional (`P⇒…⇒lhs=rhs`) rewrites, `basicSimpset[]` (19 propositional schemas), `⇒`/`∧`/`∨` congruences with context threading.
- `auto/Set.wl`: `setProve[goalTm]` + `SET[]` — set equality (α→bool with UNION/INTER/DIFF/EMPTY/UNIV) via funcExt → simpConv unfold → propTaut.
- `auto/RealArith.wl` (M7-ε, loads AFTER stdlib/Real): linear ℝ arithmetic — `realArithProve[goalTm]` (∀-closed ⇒-chains over ≤/</=/¬-atoms + ∧-hyps; opaque-atom abstraction so `realAbs u`/`x·y` become Farkas variables; ¬(=) hyps rejected) + `REALARITH[]` tactic. Engine: canonical literals `rnumTm[n]=&ℝ(&ℚ(&ℤn))` + memoized ground provers `rnum{Add,Mul,Le,Lt,NotLe,Pos,Nonneg,Ne}` (lifted through the intOfNum→ratOfInt→realOfRat iff-chains); ordered-field toolkit (`realLe/Lt…AddMono{R,2}Thm`, `realLe/LtMulCancelThm`, `realLe/LtAddCancelThm`, `realLtIrrefl`/`realNotLeLt`/`realEqIffLeLe`); proof-producing `realLinNormConv` (signed canonical, stripOrigin-keyed atom sort) + `realAtomNormConv` (×LCD via MulCancel iff + negative-part balancing via AddCancel iff → both sides nonneg-ℕ-coefficient canonical); strictness-tracked exact Fourier–Motzkin oracle → nat-scaled Farkas certificate replayed in the kernel (mul-mono scaling, add-mono-2 chain-add, renormalize, `rnumNotLe`/`realLtIrrefl` close to F). Capstone proven: `∀a b. a<b ⇒ a<(a+b)·(1/2)`.
- `stdlib/Pair.wl`: `α×β`, constructor `,`, `FST`/`SND`, pair injectivity. (`mkPair` underlying.)
- `stdlib/Sum.wl`: `α+β`, `INL`/`INR`, injectivity + disjointness.
- `stdlib/Option.wl`: `α option`, `NONE`/`SOME`, injectivity + `noneNotEqSomeThm`; `isOptionPredicate` for case analysis.
- `stdlib/Set.wl`: sets as `α→bool` — `IN`/`SUBSET`/`UNION`/`INTER`/`DIFF`/`EMPTY`/`UNIV`/`INSERT`/`SING`/`DELETE`/`POW`/`IMAGE`/`PREIMAGE`/`BALL`/`BEX`/`COMPOSE`/`I`/`INJ`/`SURJ`/`BIJ` + membership/subset theorems. Parser supports `{x | P}` set-builder.
- `stdlib/Num.wl`: ℕ from `ind`+INFINITY_AX. `0`/`SUC`/Peano/`numInductionThm`; `ITER`/`numIterationThm`; `+`/`*`/`^` (comm/assoc/distrib/cancel); `≤`/`<` (refl/trans/antisym/total); `strongInductionThm`; `wellOrderingThm`; `divisionThm`/`DIV`/`MOD`; `divides` predicate + basic arithmetic (`dividesRefl/Add/MultRight/AddRight/Leq…`); `exDiv` exact quotient via Hilbert ε (`exDivThm`/`exDivOneThm`/`exDivZeroThm`) + `multNonzeroThm` (no ℕ zero divisors). **The heavy divisibility theory — `gcd`, `prime`, `euclidLemmaThm` — moved to FTA.wl 2026-06** (`divides`/DIV/MOD/exDiv stay as ℕ-foundational).
- `stdlib/List.wl`: `α list` = `num → α option` finite-support subtype (`isListP` carrier predicate). `NIL`/`CONS`, `repNil`/`repConsHead`/`repConsTail`, `consInjThm`, `nilNotEqConsThm`, `listInductionThm`; `LENGTH`/`HD`/`TL`; `LIST_ITER_GRAPH` + `listIterationThm`; `FOLDR`/`FOLDL`/`APPEND`/`MAP`/`FILTER`/`ALL`/`MEM` (all with NIL/CONS clauses, built via `listRecExists`); `foldrAppendThm` / `allAppendThm` distributions; helpers `optionCasesThm`, tail/shift toolkit, `funcExtThm`.
- `stdlib/Finite.wl`: inductive `FINITE` (`finiteConst`/`finiteDefThm`); `finiteEmptyThm`/`finiteInsertThm`/`finiteSingThm`; strong `finiteInductThm`; closure `finiteUnionThm`/`finiteSubsetThm`/`finiteDeleteThm`/`finiteImageThm`; FINREC/ITSET/FINITE_RECURSION + `CARD`/`NSUM`. Local helpers: `propSetEq` (propositional set-eq via simpConv+propTaut+funcExt), `setExtFromInEq`, `condDeleteEqLeft`/`Right`, `imageEmptyEq`/`imageInsertEq`, INST-based membership accessors `inInsertAt`/`inDeleteAt`/`inImageAt`.
- `stdlib/FTA.wl`: **the home for ℕ divisibility theory** (boundary: heavy divisibility → FTA; Num keeps only the `divides` predicate + basic arith + DIV/MOD/exDiv). Owns (defined here): `gcd` (Hilbert-ε on universal property: `gcdExistsThm`/`gcdConst`/`gcdDefThm`/`gcdSpecThm`/`gcdDividesLeft/RightThm`/`gcdUniversalThm`), `prime` (`primeConst`/`primeDefThm`), `euclidLemmaThm` — all migrated from Num.wl 2026-06 (Num kept `divides`/DIV/MOD as foundational). Prime factorization `primeFactorsExistsThm` + `primeFactorsUniqueThm` (modulo `PERM`); ℕ multiplicative helpers `multLeftCancelThm` (`¬(x=0) ⇒ x*a=x*b ⇒ a=b`), `dividesPosThm`. (`multEqZeroThm` lives in Num.) gcd/divisibility/Bezout/coprime extended theory migrated from Rat.wl 2026-06: `dividesAntisymThm`, `divides{One,ZeroImpZero,MultBothLeft}Thm`, `gcd{Comm,Self,Rec,ZeroLeft,ZeroRight,OneRight}Thm`, `gcdNonzeroFromRightThm`, `bezoutNatThm` (disjunctive ℕ Bezout), `coprime{Reduced,DividesProduct}Thm` (Gauss), `exDivSelfThm`.
- `stdlib/Int.wl`: ℤ via canonical reps over `num × num` (carve `INT_REP = λp. FST p=0 ∨ SND p=0`; `ABS_int`/`REP_int`). Ordered integral domain: `&ℤ` embedding (`intOfNum…`); `intNeg`/`intSucc`/`intPred`; `intAdd` (comm/assoc/`intAddZeroThm`/`intAddNegThm`); `intMul` (comm/`intMulOneThm`/`intMulZeroThm`/distrib/assoc); no-zero-divisors `intMulEqZeroThm` + cancellation `intMulCancelThm`; order `intLe`/`intLt` (refl/antisym/trans/total + add/neg/mul-nonneg monotonicity); `intAbs`; bidirectional induction `intInductionThm`. Grothendieck-equivalence engine `canonEquivThm`/`canonInjThm`/`canonRespectsThm` + `repIntAddThm`/`repIntMulThm` underpin op well-definedness; ARITH (loaded just before) discharges the FST/SND-atom num glue. Magnitude/exact-division layer migrated here from Rat.wl 2026-06: `intNatAbs` (`|z| = FST(REP)+SND(REP)`: `intNatAbs{Zero,Neg,OfNum,Nonzero,MulOfNum}Thm`, `intSqNatAbsThm`), `intDivNat` (componentwise exact division by a nat: `repIntDivNatThm`/`intDivNat{One,Zero}Thm`/`intNatAbsIntDivNatThm`/`intMulDivNatCancelThm`), order cancellation `intLeMulNonnegCancelThm` + cross-product congruence `pairLeCong{Left,Right}Thm`.
- `stdlib/Rat.wl`: ℚ via canonical **reduced** fractions over `int × num` (carve `RAT_REP = λp. ¬(SND p=0) ∧ gcd (intNatAbs (FST p)) (SND p) = SUC 0`; `ABS_rat`/`REP_rat`; kernel `=` IS rational equality, **no setoid**). **DENSELY-ORDERED FIELD** (M7-6 complete, stages a–g): `&ℚ` embedding (`ratOfInt…`) — ring/order homomorphism `ratOfInt{Add,Mul,Le}Thm`; `ratAdd` (comm/assoc/`ratAddZeroThm`/`ratAddNegThm`) + `ratNeg` — additive abelian group; `ratMul` (comm/`ratMulOneThm`/`ratMulZeroThm`/distrib/assoc) + `ratInv`/`ratMulInvThm`; lowest-terms uniqueness `ratEqCrossThm`; order `ratLe`/`ratLt` (cross-mult to Int's `intLe`; linear-order `ratLe{Refl,Antisym,Trans,Total}Thm` + `ratLtNotLeThm` + compat `ratLeAddMonoThm`/`ratLeMulNonnegThm` + strict `ratLtAddMonoThm`/`ratLtMulPosCancelThm`/`ratAddSubCancelThm`; `intLeMulNonnegCancelThm`, `pairLeCong{Left,Right}Thm`); density `ratDenseThm` (`ratMulTwoThm` `x·2=x+x`). Machinery: `ratCanon` (gcd-reduction to lowest terms, built on the now-migrated `gcd` in FTA + `exDiv` in Num + `intNatAbs`/`intDivNat` in Int: `ratCanonLands`/`ratCanonId`/`ratCanonSelf`/`ratCanonZeroNum`); the **ratCanon-respects + cong tower** (`ratCanonEquiv`/`Inj`/`CrossTrans`/`Respects`, `ratAddCongLeft/Right`, `ratMulCongLeft/Right`, `repAddEquivAt`/`repMulEquivAt`/`repInvEquivAt`) is the cross-multiplication analog of Int's Grothendieck layer, underpinning op well-definedness. **M7-6 ℚ COMPLETE (stages a–g): densely-ordered field.** Housekeeping done. **Carried ℕ/int lemma migration DONE 2026-06** (cold Strict run_all 1994/0): the ℕ gcd/divisibility/Bezout/coprime theory + `exDivSelf` went to FTA.wl, `exDiv`+`multNonzero` to Num.wl, and the `intNatAbs`/`intDivNat` magnitude layer + `intLeMulNonnegCancel`/`pairLeCong{Left,Right}` to Int.wl; Rat now keeps only the rat-specific carve/ops/order + small term-builder helpers that bind the migrated public constants.
- `stdlib/Real/` (M7-7, **FOLDER** per PLAN §8.1 — files share context `HOL`Stdlib`Real`): ℝ via Dedekind cuts (single lower set `L:rat→bool`; kernel `=` is real equality, no setoid).
  - `Real/Cut.wl` **DONE**: `IS_CUT` (4 conds) + carve `real`/`ABS_real`/`REP_real` (+`absRepRealThm`/`repAbsRealThm`/`repRealIsCutThm`); principal-cut `principalCutIsCutThm` (∀q. IS_CUT {p:p<q}) + witness `isCutZeroWitnessThm`; cut accessors `realNonempty/Proper/DownClosed/OpenThm`; order `realLe` (inclusion) `realLe{Refl,Trans,Antisym,Total}Thm` + `realLt`/`realLtNotLeThm`. Parked reusable strict-order lemmas (logically Rat/Int): `intOfNumLtThm`/`ratOfIntLtThm`/`ratLtIrreflThm`/`ratLtTransThm`/`ratLeCasesThm`.
  - `Real/Field.wl` **Stages A+B+C DONE** (additive abelian GROUP; cold-Strict run_all 2125/0): **A** `&ℝ` embedding (`realOfRat{Def,Mem,Inj,Le}`); **B** `realAdd` (`sumCutIsCut`/`realAddMem`/comm/assoc/`realAddZero`); **C** `realNeg` (Rudin; `negCutIsCut`/`realNegMem`) + `realAddNegThm` (`x+(−x)=0`). Parked ℚ/ℤ vocab (→ future `Real/RatAux.wl`): `ratNeg{Neg,Add}Thm`/`ratAdd{Left,Right}CancelThm`/`ratNegPosThm`, `intArchThm`/`ratArchThm`/`intL{t,e}LtTransThm`, `ratMulRightCancelThm`/`ratLtMulPosThm`/`ratNatMulGtThm`, `cutStraddleThm`/`ratNatSucMulThm`, `memNotMemLtThm` (since extracted to RatAux.wl). **GRADUATED into bootstrap.mx 2026-06-10.**
  - `Real/RatAux.wl` **DONE** (2026-06-07, pre-mul cleanup; dev.wls real 131/0): **ALL** `rat*`/`int*` pure-ℚ/ℤ aux vocabulary extracted verbatim from Field.wl — the Archimedean + ℚ mul-order layer (`intLtLeTrans`/`intLeLtTrans`/`intArch`/`ratArch`/`ratMulRightCancel`/`ratLtMulPos`/`ratNatMulGt`/`ratNatSucMul`) AND the additive ℚ algebra (`ratAddSubCancelLeft`/`ratAddSubLeft`/`ratSubAdd`/`ratSubSub`/`ratAddRightCancel`/`ratSubSubDist`/`ratSubAddDist`/`ratNegNeg`/`ratAddLeftCancel`/`ratNegAdd`/`ratSubLtSelf`/`ratLtSubPos`/`ratNegPos`) + builders (`ratSubTm`/`natMulQ`/`ratNatZeroMul`/…). Field 1669→1099; **no `rat*` def left in Field** (it consumes them from RatAux). Loads **Cut→RatAux→Field→Mul** (after Cut — needs Cut's shared vocab).
  - `Real/Mul.wl` **Stage D COMPLETE — Layers 1+2** (cold Strict run_all 2181/0): **L1 (nonneg core)** `realNnMul` (strict `r<p·q` ∨ `r<0` cut body) + `nnMulCutIsCutThm` (4 conds) + rep/mem; extraction-based membership helpers `nnMemEq`/`nnMemIntroL`/`nnMemIntroR`/`nnMemPosThm`/`nnMemProdThm` (capture-proof — all ∃-terms from the kernel); `realNnMul{Comm,Nonneg,Zero,One,AssocNonneg,DistribNonneg}Thm`. **L2 (signed)** `realMul` = nested `COND` binary `0≤x`/`0≤y` + case lemmas `realMulCase{PP,PN,NP,NN}Thm` + `splitSign` combinator; realNeg algebra `realNeg{Zero,Neg,Add}Thm`/`realLeNegThm`/`realNeg{Nonneg,Nonpos}Thm`/`realAddCancelLeftThm`; sign homomorphism `realMulNeg{Right,Left}Thm`; `realMul{Zero,One,Comm,Assoc}Thm`; **`realMulDistribThm`** (signed, the no-reference step) via `distribNonnegX`/`mixedCase`/`nnNegCase` + `mulNegLeft`/`realNegAdd` peel; order `realMulNonnegThm`=`realLeMulNonnegThm`/`realLtMulPosThm` + `notLeWitnessThm`/`realPosHasPosMemThm`. **Ordered-field ×/≤ compatibility surface** (2026-06-10): `realLe/LtAddMonoThm`, bridges `realLeSubNonnegThm`(`a≤b⟺0≤b−a`)/`realLtSubPosThm`, `realLeMulMonoThm`/`realLtMulMonoThm`. **Stage E `&ℝ` ring/order homomorphism** (2026-06-10): `realOfRatAddThm`/`realOfRatNegThm` + nonneg core `realOfRatNnMulThm` + signed `realOfRatMulThm` (4 sign cases via realMulCase + ℚ `ratMulNeg{R,L}`/`ratMulNegNeg`); `realOfRatLeThm` was already in Field. Detail: PLAN Phase-3. **GRADUATED into bootstrap.mx 2026-06-10 (no longer frontier).**
  - `Real/Inv.wl` **DONE — ℝ IS A FIELD** (cold Strict run_all 2200/0; NO Lean blueprint — from-scratch Rudin reciprocal): **positive core** `invPos` (cut body `{p | ∃w. ¬REP_x w ∧ 0<w ∧ (p·w)<1}`) + `invPosCutIsCutThm` (4 conds under `0<x`) + rep/mem + `invPosNonnegThm`; **`invPosMulThm`** (`0<x ⇒ realNnMul x (invPos x) = &ℝ1`, the hard Archimedean/`cutStraddle` ⊇ half — gap `< s0·(1−r)` so boundary `y/(y+gap)>r`, witness `(y, t∈(r/y,1/(y+gap)))`); **signed** `realInv` = `COND (realLt 0 x) (invPos x) (realNeg(invPos(realNeg x)))` + `realInvPos/NegThm` + `realNegPosThm`; **`realMulInvThm`** (`¬(x=&ℝ0) ⇒ realMul x (realInv x) = &ℝ1`) via the `realMulNeg` sign homomorphism. Ordered-field ×/≤ compat + Stage E + graduation all DONE 2026-06-10 (see the Mul.wl entry). **GRADUATED into bootstrap.mx 2026-06-10.**
  - `Real/Complete.wl` **DONE — ℝ IS A COMPLETE ORDERED FIELD** (2026-06-10, cold Strict run_all 2250/0; Lean blueprint Cut/Sup.lean for sup, rest from scratch): strict-order vocabulary `realLtImpLeThm`/`realLtLeTransThm`/`realLeLtTransThm`/`realLtTransThm`/`realOfRatLtThm`; `realSup` (= `ABS_real (λq. ∃a. S a ∧ REP_real a q)`, union of member lower sets) + `supCutIsCutThm`/`repRealSupThm`/`realSupMemThm` (all conditional on nonempty + bounded-above) + `realSupUpperThm`/`realSupLeastThm`/**`dedekindCompleteThm`**; `realRatBoundThm`/**`realArchThm`** (Archimedean)/**`realDenseThm`** (ℚ dense in ℝ). Private idiom `chooseBody` (kernel-extracted CHOOSE hypothesis instance). Tests: `tests/real_complete_tests.wl`.
  - `Real/Abs.wl` + `Real/MinMax.wl` **DONE — M8-prep order toolkit** (2026-06-11, cold Strict run_all 2306/0; both authored end-to-end by Codex from `codex/brief-003`/`-004`, zero-debug): `realAbs` (COND def + `realAbsPos/NegCase/Zero/Nonneg` + `realLeAbsSelfThm`/`realNegLeAbsThm` + `realAbsNegThm` + **`realAbsTriangleThm`**); `realMax`/`realMin` (COND defs + case lemmas + bound laws `realLeMax*`/`realMinLe*` + `realMaxLubThm`/`realMinGlbThm` + comm via antisym + **`realAbsMaxThm`** `|x| = max x (−x)`). Tests: `tests/real_abs_tests.wl`, `tests/real_minmax_tests.wl`.
